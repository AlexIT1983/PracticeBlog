// Наши редьюсер для post

const initialPostState = {}; // Начальное состояние

// сам редьюсер
export const postReducer = (state = initialPostState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
